services:
  postgres:
    image: postgres:16
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB : banque
    ports:
      - "5432:5432"
    volumes :
      - ${PWD}/DATA:/var/lib/postgresql/data
